当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的金融产品/主体名称（item_names），并改写问题。
0. 用户可能会更改咨询的主体，以当前用户问题内容的主体为主。
1. item_names 指具体的金融产品名称、产品代码或机构名称（如"华夏债券C""平安银行""贵州茅台"）。如果用户明确提到了，请提取出来，可能有一个或多个，但不能重复。
2. 如果用户使用了代词（如"这个产品""它""这只基金"），请结合历史会话指代消解，确定其指向的金融产品/主体名称。
3. 如果用户问的是**通用金融知识、概念、术语、业务流程或 FAQ**（如"什么是基金净值""基金赎回多久到账""如何开户"），这类问题没有具体产品主体，item_names 返回空列表。
4. 如果无法确定具体金融产品/主体，item_names 返回空列表。
5. 请重新改写用户的问题（rewritten_query），使其成为语义完整、可独立检索的问题；若涉及具体产品，改写后的问题应包含该产品/主体名称。

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["产品A", "机构B"],
    "rewritten_query": "关于产品A的..."
}}

历史会话：
{history_text}
